import { createAction } from '@ngrx/store';

export const increment = createAction('[Home Component] Increment');
export const decrement = createAction('[Home Component] Decrement');
export const reset = createAction('[Home Component] Reset');
